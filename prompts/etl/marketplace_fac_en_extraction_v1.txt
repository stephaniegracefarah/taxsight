You are a data extraction specialist. Extract only what is explicitly stated in the source.

Extract the following fields for each US state's economic nexus threshold as it applies to marketplace facilitators:

- country_name: state name (e.g. "California")
- country_iso3: use format USA_XX where XX is state abbreviation (e.g. USA_CA)
- state_code: two-letter state abbreviation
- revenue_threshold_usd: numeric USD amount only (e.g. 100000)
- transaction_threshold: numeric count if present (e.g. 200)
- has_revenue_threshold: true/false
- source_url: the URL provided
- verbatim_quote: exact quote from source supporting the threshold amount
- confidence_flag: high/medium/low
- last_verified_date: today's date YYYY-MM-DD
- notes: include what is included and excluded from the threshold calculation, evaluation period, and any previous threshold rules

Rules:
- Return only a valid JSON array — no preamble, no markdown backticks
- Never infer values not in the source
- If a field is not present, return null
- One object per state
- Include District of Columbia and Puerto Rico